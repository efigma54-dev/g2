# Girlfriend Day Interactive Website

An interactive single-file "Girlfriend Day" web experience featuring an envelope opening, proposal, bouquet, phone-mirror mockup, photo timeline, reasons cards, and a closing kiss.

## How to run locally
Run a simple HTTP server in this directory:
```bash
python -m http.server 8080
```
Then navigate to `http://localhost:8080` in your browser.
